# ── Python 버전 요구사항 ──────────────────────────────────────────────────────
# Python >= 3.10 필수

# ── Pi Camera v2 / 이미지 캡처 ───────────────────────────────────────────────
# picamera2는 Raspberry Pi OS에 기본 포함.
# 별도 pip 설치 시: sudo apt install -y python3-picamera2
# picamera2               # apt 설치 권장 (pip 설치 시 libcamera 의존성 문제 발생 가능)

# ── 이미지 처리 ──────────────────────────────────────────────────────────────
Pillow>=11.0.0            # 이미지 리사이즈 / 포맷 변환
numpy>=2.2.6              # 배열 처리 (picamera2 내부 의존)

# ── Gemini Vision API ─────────────────────────────────────────────────────────
google-generativeai>=0.8.6

# ── MQTT 브리지 ──────────────────────────────────────────────────────────────
paho-mqtt>=2.1.0

# ── 환경변수 관리 ─────────────────────────────────────────────────────────────
python-dotenv>=1.2.2

# ── 로깅 / 유틸리티 ──────────────────────────────────────────────────────────
colorlog>=6.10.1          # 컬러 터미널 로그 출력